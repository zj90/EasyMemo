<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="MyTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/MyActionBarStyle</item>
        <item name="android:actionMenuTextAppearance">@style/MenuTextSize</item>
        <!-- <item name="android:homeAsUpIndicator">@drawable/ic_home_back</item> -->

    </style>


    <style name="MyActionBarStyle" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/deep_blue</item>
        <item name="android:titleTextStyle">@style/MyTitleTextStyle</item>
        <item name="android:displayOptions">showHome|showTitle</item>
    </style>

    <style name="MyTitleTextStyle">
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="MenuTextSize">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="WelcomeTheme" parent="android:Theme.Light.NoTitleBar">
        <item name="android:windowAnimationStyle">@style/WelcomeAnimation</item>
    </style>

    <style name="WelcomeAnimation" parent="@android:style/Animation.Activity"></style>

</resources>